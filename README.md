# Resurrect vault

This directory is a snapshot of an Omarchy machine, written by
[Resurrect](https://github.com/tsouth89/omarchy-resurrect).

Replay it onto a fresh install with:

```bash
ress restore --vault /tmp/demo-vault
```
